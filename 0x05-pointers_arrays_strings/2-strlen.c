#include "main.h"
/**
 * _strlen - prints the length of strings
 * @*s: argument to be printed.
 * Return:void
 *
*/

int _strlen(char *s)
{
	char *s;
	int i = 0;

	s =str;
	while (*s != '\0') {
		i++;
		s++;
	}
	_putchar(%d,i);
	return (0);
}

