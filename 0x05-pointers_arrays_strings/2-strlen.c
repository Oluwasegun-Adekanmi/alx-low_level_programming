#include "main.h"
/**
 * _strlen - prints the length of strings
 * @*s: argument to be printed.
 * Return:void
 *
*/

int _strlen(char *s)
{
	int count =0;

	while (*(s+count) != 0)
	{
		count++
	}
	return (count);
}

