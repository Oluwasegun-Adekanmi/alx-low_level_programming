#include <stdio.h>
#include <unistd.h>
/**
 * main - Entry point into the program
 *
 * Return: Always 0 (success)
 *
**/

int main(void)
{
	char i;
	char r;

	for
		(i = 'a'; i <= 'z'; i++);
	{
		putchar(i);
	}

	for
		(r = 'A'; r <= 'Z'; r++);
	{
		putchar(r);
	}
	putchar('\n');
	return (0);
}
