#include <stdio.h>
/*
 * main - serves as the entry point gor the program
 * return: By default returns zero if no error is
 * found otherwise returns non-zero
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
