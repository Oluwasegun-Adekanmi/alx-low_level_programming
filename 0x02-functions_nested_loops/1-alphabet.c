#include "main.h"
/**
 * main - Entry point for the program
 *
 * Return: Always 0 (success)
 *
*/

void print_alphabet(void)
{
	char lowcase;

	for
	       (lowcase = 'a');
	       (lowcase <= 'z');
		lowcase++;

	{
		_putchar(lowcase);
	}
		_putchar('\n');

	return (0);
}
